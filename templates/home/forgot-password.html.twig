{% extends 'layouts/base-home.html.twig' %}
{% block title %}Esqueceu sua senha{% endblock %}

{% block body %}
<div class="bg-light min-vh-100 d-flex flex-row align-items-center">
    <div class="container">
        {% for message in app.flashes('resetPasswordSent') %}
            <div class="row justify-content-center">
                <div class="col-7 p-0">
                    <div class="card border-top-success text-success mb-3">
                        <div class="card-header">Redefinição de senha</div>
                        <div class="card-body">
                            <h5 class="card-title d-flex gap-2 align-items-center">
                                <span class="text-warning">
                                    <svg class="icon icon-lg" version="1.1" xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24"><path d="M0.75 5.25v18h22.5v-18zM11.1 16.013c0.248 0.186 0.561 0.298 0.9 0.298s0.652-0.112 0.904-0.301l-0.004 0.003 1.1-0.825 7.75 5.813v0.75h-19.5v-0.75l7.75-5.813zM12 14.813l-9.75-7.313v-0.75h19.5v0.75zM2.25 9.375l6.5 4.875-6.5 4.875zM21.75 19.125l-6.5-4.875 6.5-4.875z"></path></svg>
                                </span>
                                {{ message }}
                            </h5>
                            <p class="card-text">
                                <div>Enviamos a você um e-mail com o link para concluir a redefinição da sua senha.</div>
                                <div>Não localizou o e-mail? Verifique a sua caixa de Spam.</div>
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        {% endfor %}
        <div class="row justify-content-center">
            <div class="col-lg-7">
                <div class="card-group d-block d-md-flex row">
                    <div class="card col-md-7 p-4 mb-0">
                        <div class="card-body">
                            {{ form_start(form) }}
                                <div class="d-flex align-items-end gap-3 mb-3">
                                    <img class="sidebar-brand-full" src="{{ asset('images/dog-logo.svg') }}" alt="Logo PetApp" width="72">
                                    <h1>Pets<b class="text-warning">Control</b></h1>
                                </div>
                                <p class="text-medium-emphasis">Recupere acesso a sua conta</p>
                                <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}" />
                                <div class="form-floating mb-3">
                                    {{ form_widget(form.email, { 'attr': { 'class': 'form-control', 'placeholder': 'email' } }) }}
                                    {{ form_label(form.email) }}
                                    <div class="form-error text-danger mt-1">
                                        {{ form_errors(form.email) }}
                                    </div>
                                </div>
                                <div class="row mt-3">
                                    <div class="col d-flex justify-content-center">
                                        <button type="submit" class="btn btn-lg btn-primary px-4">Recuperar senha</button>
                                    </div>
                                </div>
                                <div class="row mt-3">
                                    <div class="col d-flex justify-content-center gap-1">
                                        Lembrou da senha? Faça <a class="btn-link" href="{{ path('app_login') }}">login aqui</a>
                                    </div>
                                </div>
                            {{ form_end(form) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
